{
  "name": "pail",
  "version": "1.0.0",
  "description": "file based json store for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/pail"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/pail/issues"
  },
  "homepage": "https://github.com/fishin/pail",
  "engines": {
    "node": ">=0.12.x"
  },
  "scripts": {
    "test": "lab -a code -t 100 -v -m 2000 -L -r json -o lab.json -r html -o lab.html -r console -o stdout -r console -o console.log",
    "console": "lab -a code -t 100 -v -m 2000 -L"
  },
  "dependencies": {
    "hapi": "8.x.x",
    "hoek": "2.x.x",
    "node-uuid": "1.4.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "lab": "5.x.x"
  }
}
