{
  "name": "pail",
  "version": "1.1.0",
  "description": "file based json store for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/pail"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/pail/issues"
  },
  "homepage": "https://github.com/fishin/pail",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 2000 -L",
    "docs": "jsdoc2md lib/** > api.md",
    "test": "lab -a code -t 100 -v -m 2000 -L -r json -o test.lab -r html -o test.html -r console -o stdout -r console -o test.log"
  },
  "dependencies": {
    "hoek": "2.x.x",
    "node-uuid": "1.4.x"
  },
  "devDependencies": {
    "code": "1.x.x",
    "jsdoc-to-markdown": "1.2.x",
    "lab": "7.x.x"
  }
}
